{
    "data-root": "{{ data_root }}",
    "default-runtime": "{{ default_runtime }}",
    "runtimes": {{ runtimes | to_nice_json }},
    "storage-driver": "{{ storage_driver }}",
    "storage-opts": {{ storage_opts | to_nice_json }}
}
